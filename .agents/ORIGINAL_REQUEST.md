# Original User Request

## 2026-08-30T16:58:41Z

Build the data ingestion pipeline, data validity layer, STL decomposition engine, edge-case scenario simulators, telemetry hooks, and frontend upload page for the Business Intelligence Engine. Do not spawn more than 6 agents in total.

Working directory: c:\Users\Abhay\Desktop\CODING\BuisnessIntelligence.ai
Integrity mode: development

Reference material: `BI_ENGINE_IMPLEMENTATION_PLAN.md` in the working directory root (968-line architectural specification). Read it thoroughly before implementation. **Do NOT edit this file.**

## Critical Constraints

- **Do NOT push, commit, or interact with git.**
- **Do NOT edit `BI_ENGINE_IMPLEMENTATION_PLAN.md`.**
- **Do NOT modify any existing files under `kpi-engine/` unless explicitly required for integration** (e.g., adding an import, registering a route). If you must modify an existing file, make the smallest possible change.
- **Do NOT follow Section 6.1's directory structure.** The user has specified a custom directory layout (see below).
- **Use `statsmodels.tsa.seasonal.STL`** for the STL decomposition engine rather than reimplementing Cleveland's algorithm from scratch.
- **Use third-party libraries** for complex math wherever possible (scipy, numpy, statsmodels, pandera, etc.) to avoid latency and bugs.
- **Flag all mock/synthetic data** clearly in console output, NOT in code comments or the repo. When any piece of code uses simulated data, its runtime output must print a clear notice like: `[MOCK DATA] This output uses synthetic/simulated data. Replace with real ingested data.`
- **LangSmith integration**: The existing `kpi-engine/app/monitoring/` directory has LangSmith scaffolding (tracing.py, feedback.py). Do NOT replace or remove these files. For LLM-related telemetry (hooks 3, 5, 7), LangSmith will auto-capture when enabled. For non-LLM telemetry (hooks 1, 2, 4, 6), use simple `time.perf_counter()` decorators. Aggregate both into a `TelemetryCollector`.
- **Supabase SQL**: Output any DDL/DML SQL statements (table creation, modifications) to the console. Do NOT execute them against Supabase directly.
- **MinIO connection**: API at `http://localhost:19000`, credentials `minioadmin`/`minioadmin`. Console at `http://localhost:19001`.

## User's Custom Directory Structure

```
BuisnessIntelligence.ai/
├── data-ingest/                    # NEW: Bronze + Silver ingestion pipeline
│   ├── pipeline.py                 # Bronze (MinIO WORM) → Silver (Polars normalize)
│   ├── bronze.py                   # MinIO raw payload storage
│   ├── silver.py                   # Polars vectorized cleansing, type casting, regularization
│   └── imputation.py               # Time-series regularization & Akima/seasonal imputation (§2.5)
├── data-validity/                  # NEW: Validation, DQ scoring, quarantine, telemetry schemas
│   ├── validation.py               # Tier 1 (Pydantic V2) + Tier 2 (Pandera) + Tier 4 (Boundary) + Tier 6 (Drift/KS-test)
│   ├── quarantine.py               # Dead-letter quarantine store & replay API (§2.3)
│   ├── scoring.py                  # Composite DQ scoring algorithm (§2.4)
│   ├── golden_datasets.py          # GoldenDatasetSpec schema & 4-tier catalog generation (§5.1)
│   ├── benchmark_runner.py         # CI/CD regression evaluation harness (§5.2)
│   └── telemetry/                  # Telemetry sub-directory
│       ├── collector.py            # TelemetryCollector class aggregating all hooks (§5.3)
│       ├── pricing.py              # Dynamic model token pricing & cost calculator (§5.3)
│       └── hooks.py                # Non-blocking perf_counter decorators for hooks 1,2,4,6 (§5.4)
├── edge_cases/                     # NEW: Simulated KPI scenario data & runners
│   ├── multifactor.py              # Scenario 1: Multi-factor KPI with Shapley attribution (§4.1)
│   ├── low_confidence.py           # Scenario 2: Low-confidence with composite C_composite (§4.2)
│   ├── sparse_history.py           # Scenario 3: Cold-start Bayesian prior borrowing (§4.3)
│   └── role_security.py            # Scenario 4: Role-based security & PII masking (§4.4)
├── kpi-engine/                     # EXISTING: Do not restructure
│   ├── app/
│   │   ├── api/
│   │   │   ├── middleware.py       # Hook 1: TelemetryMiddleware (§5.4) — CREATE if not exists
│   │   │   └── routes.py          # Add /ingest and /quarantine/replay routes
│   │   ├── orchestrator/
│   │   │   └── ...                # EXISTING: graph.py, nodes.py — integrate STL upstream
│   │   ├── timeseries/            # NEW sub-directory under kpi-engine/app/
│   │   │   ├── stl.py             # STL decomposition using statsmodels (§3.1-3.5)
│   │   │   ├── baseline.py        # Dynamic expected Ŷ_t and MAD uncertainty σ_R (§3.5)
│   │   │   ├── anomaly.py         # Z-score calculation & KPIMovementEvent trigger (§3.5)
│   │   │   └── parameters.py      # Cadence parameter matrix for 5 business cadences (§3.4)
│   │   └── schemas/
│   │       └── ...                # Add ingestion.py, timeseries.py, telemetry.py schemas
│   └── ...
└── frontend/
    └── Dashboard/
        └── src/
            └── ...                # Add Upload Documents page with 2-column layout
```

Files like `middleware.py` that logically belong in `kpi-engine/app/api/` should go there — use common sense for placement based on what the file does, not based on which "layer" the user described it under.

## Requirements

### R1. Data Ingestion Pipeline (§2.1 Bronze + Silver + §2.5 Imputation)

Build the `data-ingest/` directory implementing:
1. **Bronze Layer**: Store raw uploaded payloads immutably in MinIO (endpoint `http://localhost:19000`, credentials `minioadmin`/`minioadmin`). Partition by `tenant_id/kpi_id/YYYY/MM/DD/`. Use `boto3` or `minio` Python client.
2. **Silver Layer**: Use Polars to normalize, type-cast, regularize timestamps to ISO-8601 UTC, and standardize dimension hashes.
3. **Imputation** (§2.5): Implement time-series regularization with gap-filling strategy: Akima spline for g≤3, seasonal persistence for 3<g≤period, rejection for g>20% of series length. Mark imputed records with `is_imputed=True`.
4. **SQL DDL**: Print the `canonical_measurements` DDL (from §2.1) and any ALTER statements needed for the existing table to the console.

### R2. Data Validity Layer (§2.2 Tiers 1,2,4,6 + §2.3 + §2.4)

Build the `data-validity/` directory implementing:
1. **Tier 1 (Pydantic V2)**: Structural & type validation — UUIDs, non-null fields, ISO-8601 parsing, dimension key sanitization.
2. **Tier 2 (Pandera)**: Columnar & statistical range validation — implement actual Pandera DataFrameSchema with registered category checks.
3. **Tier 4 (Boundary)**: Physical domain constraints — non-negative checks for count/currency, bounded ratios [0,1], dynamic 6-sigma outlier screening.
4. **Tier 6 (Drift)**: Distributional drift detection using scipy's KS-test (two-sample) and PSI calculation against a 30-day rolling baseline.
5. **Quarantine** (§2.3): Dead-letter store. Print the `quarantine_measurements` DDL to console. Implement replay API logic.
6. **DQ Scoring** (§2.4): Composite weighted score DQ ∈ [0,1] with the 5 weights specified. Map to VALID/DEGRADED/INVALID status for GoRules Rule 23.

**Intentionally skipped by user** (do NOT implement):
- Tier 3 (Temporal Continuity) — handled by existing temporal checks + §2.5 imputation.
- Tier 5 (Additive Dimensional Reconciliation) — no multi-dimensional slice data yet.

### R3. STL Decomposition Engine (§3, entirety)

Build `kpi-engine/app/timeseries/` implementing the full STL pipeline:
1. Use `statsmodels.tsa.seasonal.STL` as the core decomposition engine. Configure it with the cadence-specific parameter matrix from §3.4.
2. Compute dynamic expected baseline Ŷ_t = T_t + S_t.
3. Compute robust residual uncertainty σ_R using MAD (1.4826 × median(|R_t - median(R_t)|)).
4. Compute 99% confidence interval bands using z=2.576.
5. Compute Z-score and emit KPIMovementEvent when |Z_t| >= 2.576 AND percentage delta >= 5% materiality threshold.
6. Implement the Pydantic schemas from §3.6 (STLParameters, TrendDataPoint, STLDecompositionResult).
7. Handle edge cases: sparse history diversion, missing data imputation, multiplicative series via log transform (§3.7).
8. Create the synthetic 90-day verification wave from §3.8 and run it to validate orthogonality and anomaly detection.

### R4. Edge-Case Scenario Simulators (§4.1-4.4)

Build the `edge_cases/` directory with **simulated/synthetic data** for each scenario. These are critical and will be shown in the frontend. Design the output structures carefully.
1. **Scenario 1 (Multi-factor)**: Simulate 3 concurrent drivers. Implement Shapley value attribution. Use `itertools` for coalition enumeration. Validate attribution sums to total delta.
2. **Scenario 2 (Low-confidence)**: Simulate contradictory evidence. Compute composite confidence C_composite. Generate structured clarification payload. Map to GoRules Rule 20/21/22.
3. **Scenario 3 (Sparse history)**: Simulate a newly launched KPI with N<14 days. Implement Bayesian prior borrowing with shrinkage factor. Widen confidence intervals by κ(N).
4. **Scenario 4 (Role-based security)**: Implement SecurityContext model, demonstrate PII masking and margin redaction based on role entitlements.

All output from these must print `[MOCK DATA]` notices at runtime.

**REMINDER for user**: The edge_cases/ directory currently uses synthetic data. When real data is available in MinIO or Supabase, revisit this directory to wire it to actual ingested data.

### R5. Telemetry Hooks (§5.1-5.4)

Build telemetry under `data-validity/telemetry/` and `kpi-engine/app/api/middleware.py`:
1. **Golden Datasets** (§5.1): Implement `GoldenDatasetSpec` Pydantic schema and generate a representative catalog of benchmark incidents.
2. **Benchmark Runner** (§5.2): Build a harness that can run golden datasets against the engine and compute Driver Recall, Attribution MAE, Abstention Precision, Security Leakage Rate.
3. **Telemetry Collector** (§5.3): Build `TelemetryCollector` class that aggregates latency, model calls, token usage, and cost. Include the dynamic pricing matrix for gpt-4o-mini, gpt-4o, and claude-3-5-sonnet.
4. **Hooks** (§5.4): Implement non-LLM hooks (1, 2, 4, 6) as `time.perf_counter()` decorators. For LLM hooks (3, 5, 7), document that LangSmith auto-captures these. Create `middleware.py` with `TelemetryMiddleware` for Hook 1 if it doesn't exist.
5. Do NOT replace or modify existing `kpi-engine/app/monitoring/` files (tracing.py, feedback.py, example_trace.py).

### R6. Frontend Upload Documents Page

Add a new route/page to the existing React Dashboard (`frontend/Dashboard/src/`):
1. Under the "Upload Documents" sidebar link, render a page with **2 columns**:
   - **Column 1: Unstructured Data** (examples: PDFs, Word docs, emails, images)
   - **Column 2: Structured Data** (examples: CSV, Excel, JSON, Parquet)
2. Each column has a drag-and-drop upload zone with file type hints.
3. On upload, the file should POST to the ingestion API endpoint (`/api/v1/metrics/ingest`). For now, display a success/error toast. The actual backend wiring will come later.
4. Use the existing Tailwind CSS styling (slate palette, blue-600 accents, clean flat design, no glassmorphism).
5. Register the route under the existing React Router setup in App.jsx.

## Acceptance Criteria

### Code Quality
- [ ] All Python files are syntactically valid and importable.
- [ ] All Pydantic V2 schemas use `model_validator` / `field_validator` where appropriate.
- [ ] Pandera schema in Tier 2 is a real `DataFrameSchema`, not a stub.
- [ ] STL decomposition produces correct trend/seasonal/residual components when run against the 90-day synthetic wave (§3.8).
- [ ] Edge-case simulators produce structured output matching the schemas defined in the plan.

### Integration
- [ ] MinIO Bronze layer successfully stores a test payload to `http://localhost:19000`.
- [ ] SQL DDL for `canonical_measurements` and `quarantine_measurements` is printed to console.
- [ ] The STL engine can be invoked independently with a pandas Series and cadence string.
- [ ] TelemetryMiddleware integrates with FastAPI without breaking existing routes.
- [ ] Frontend Upload Documents page renders correctly in the existing Dashboard app.

### Mock Data Transparency
- [ ] Every piece of simulated/synthetic data prints `[MOCK DATA]` at runtime.
- [ ] No hardcoded fake outputs masquerading as real results.
